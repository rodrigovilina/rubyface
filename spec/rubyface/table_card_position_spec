# frozen_string_literal: true

RSpec.describe Rubyface::TableCardPosition do
  describe 'values' do
    # rubocop:disable RSpec/Be
    it 'has five values', :aggregate_failures do
      expect(described_class::Left).to be
      expect(described_class::Center).to be
      expect(described_class::Right).to be
    end
    # rubocop:enable RSpec/Be
  end
end
